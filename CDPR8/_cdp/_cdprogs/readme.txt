The "test" files are physical modelling SCORES,
which use the other files (e.g. "trumpetvalve.m")
as their input instruments.

Place these files in your base directory for the Loom/CDP,
select one of them, then use the Physical Modelling "View/Edit"
options on the "Music Testbed".